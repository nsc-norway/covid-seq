FROM continuumio/miniconda3:latest

RUN apt-get update && apt-get install -y procps
RUN conda create -c bioconda -n checkvariant pyvcf pandas seaborn bedtools
RUN echo "source activate checkvariant" > ~/.bashrc
